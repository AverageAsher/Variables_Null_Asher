namespace Variables_Null_Asher
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello, World!");

            int favoriteNumber = 0;
            // The favorite number //
            bool isRunning = false, isJumping = false;
            // initializes false for jumping and running //
            float grade = 99.99f;
            // holds information about the grade //
            const double finalGrade = 88.88;
            // holds the final grade //

            Console.WriteLine("Hello, World!");
            Console.WriteLine(favoriteNumber);
            // writes to the console //
            Console.WriteLine(isRunning);
            // writes to the console //
            Console.WriteLine(isJumping);
            // writes to the console //
            Console.WriteLine(grade);
            // writes to the console //
            Console.WriteLine(finalGrade);
            // writes to the console //

        }
    }
}  